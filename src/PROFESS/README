This package provides a 'fix profess' command which is a wrapper for the
PROFESS code, so that molecular dynamics can be run with LAMMPS
using forces from orbital-free density functional theory.

The package design is adapted from that used by the LATTE package.
